mermaid
classDiagram
    class User {
        +String patient_id
        +String full_name
        +String email
        +String phone
        +Int age
        +String gender
        +String password_hash
        +DateTime reg_date
        +DateTime created_at
        +DateTime updated_at
        +String assigned_doctor_id
        +String assigned_doctor_name
        +String assigned_doctor_email
        +String assigned_doctor_mobile
        +String assigned_doctor_specialty
        +DateTime assigned_at
    }

    class Prediction {
        +String id
        +String user_email
        +String user_name
        +String patient_id
        +Int age
        +String gender
        +Map predictions
        +String image_hash
        +DateTime created_at
        +String doctor_name
        +String doctor_email
        +String doctor_mobile
        +String doctor_specialty
    }

    class Doctor {
        +String id
        +String name
        +String email
        +String mobile_num
        +String specialization
    }

    class Counter {
        +Int patient_counter
    }

    class ChatLog {
        +String id
        +String user_email
        +String user_name
        +Boolean is_admin
        +String question
        +String answer
        +String status
        +String model
        +DateTime created_at
    }

    User "1" --> "0..*" Prediction : has
    Doctor "1" --> "0..*" User : assigned_to
    User "1" --> "0..*" ChatLog : creates
    Counter ..> User : generates patient_id
    Prediction ..> Doctor : stores doctor snapshot